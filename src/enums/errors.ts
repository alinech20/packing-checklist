export enum ERROR_LEVEL {
  CRITICAL,
  ERROR,
  WARNING,
  INFO,
  LOG,
  VERBOSE,
}

export enum MESSAGE_COLOR {
  ERROR = 'darkred',
  INFO = 'darkblue',
  WARNING = 'darkorange',
  VERBOSE = 'darkslategray',
  SUCCESS = 'darkgreen',
}

export enum LOGGER_LEVEL {
  CRITICAL,
  ERROR,
  WARNING,
  INFO,
  LOG,
  VERBOSE,
}
