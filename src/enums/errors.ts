export enum ERROR_LEVEL {
  CRITICAL,
  ERROR,
  WARNING,
  INFO,
  LOG,
  VERBOSE
}

export enum MESSAGE_COLOR {
  ERROR = 'red',
  INFO = 'orange',
  VERBOSE = 'grey',
  SUCCESS = 'green',
}

export enum LOGGER_LEVEL {
  CRITICAL,
  ERROR,
  WARNING,
  INFO,
  LOG,
  VERBOSE
}
